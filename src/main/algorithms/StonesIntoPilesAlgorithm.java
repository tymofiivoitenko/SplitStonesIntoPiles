package main.algorithms;
/**
 * @author tymofiivoitenko
 */
public interface StonesIntoPilesAlgorithm {

    // Divide set into two subsets such that the difference of subset sums is minimum
    // Return minimal difference
    int getMinimalDifference(int[] stones);
}
